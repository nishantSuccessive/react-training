import React from 'react'

export default function DefaultPage() {
    return (
        <div>
            <header style={{textAlign: "center"}}>
            <h1>DEFAULT PAGE</h1>
            <p>This is default page. You should be redirected into this page by default.</p>
            </header>
        </div>
    )
}
